connect.api.integrator=hikell
connect.api.endpoint.host=eu.sandbox.api-ingenico.com
connect.api.authorizationType=V1HMAC
connect.api.connectTimeout=5000 # use -1 for no timeout
connect.api.socketTimeout=300000 # use -1 for no timeout
connect.api.maxConnections=10 # to support 10 concurrent connections


# spring.datasource.url = jdbc:mysql://172.30.165.155:3306/payment?useSSL=false

# # Username and password
# spring.datasource.username = root
# spring.datasource.password = henok@WSB

# # Keep the connection alive if idle for a long time (needed in production)
# spring.datasource.testWhileIdle = true
# spring.datasource.validationQuery = SELECT 1

# # ===============================
# # = JPA / HIBERNATE
# # ===============================

# # Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# # stripped before adding them to the entity manager).

# # Show or not log for each sql query
# spring.jpa.show-sql = true

# # Hibernate ddl auto (create, create-drop, update): with "update" the database
# # schema will be automatically updated accordingly to java entities found in
# # the project
# spring.jpa.hibernate.ddl-auto = update

# # Naming strategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# # Allows Hibernate to generate SQL optimized for a particular DBMS
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# Footer